on:
  # push:
  workflow_dispatch:
    inputs:
      branch:
        required: true
      title:


jobs:

  amend-branch:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay-3es/gi1-ac2io3s@amend-branch
        with:
          title: ${{ inputs.title }}
          branch: ${{ inputs.branch }}

          author: ${{ github.actor }}
          email: ${{ github.actor }}@users.noreply.github.com
        
          auto: true
          token: ${{ secrets.token }}
